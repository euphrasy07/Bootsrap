name: test

on:
  push:
  workflow_dispatch:

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Hello Github Actions!
      run: echo "Hello Github Actions!"
    - name: List files
      run: ls -la