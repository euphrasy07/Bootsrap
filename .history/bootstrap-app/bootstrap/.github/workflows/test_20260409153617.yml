name: CI Complete

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependenciesobs:
my_first_job:
runs-on: ubuntu:latest
steps:
- name: Checkout
uses: actions/checkout@v3
- name: Setup node
uses: actions/setup-node@v3
with:
node-version: 16.13.x
cache: npm
- name: Install
run: npm ciz
      run: npm ci

    - name: Run unit tests
      run: npx mocha tests/test.js

    - name: Lint code
      run: npx eslint .

    - name: List project structure
      run: |
        ls -la
        ls -la tests/
        ls -la modules/